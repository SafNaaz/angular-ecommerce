export class Product {
  sku: string;
  name: string;
  description: string;
  unitPrice: number;
  imangUrl: string;
  active: boolean;
  unitsInStock: number;
  dataCreated: Date;
  lastUpdated: Data;
}